import { Node } from "mdast";
import { TagPlugin, PluginContext } from "../tag-plugin";
export interface RawProps {
    children: any;
}
export declare const Tags: string[];
export declare class RawPlugin extends TagPlugin {
    transform(_props: Record<string, any>, children: Node[], context: PluginContext): Promise<Node[] | Node>;
}
