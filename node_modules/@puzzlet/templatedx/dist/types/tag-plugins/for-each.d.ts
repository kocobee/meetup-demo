import { Node } from 'mdast';
import { TagPlugin, PluginContext } from '../tag-plugin';
export declare const Tags: string[];
export interface ForEachProps<T = any> {
    children: (item: T, index: number) => any;
    arr: Array<T>;
}
export declare class ForEachPlugin extends TagPlugin {
    transform(props: Record<string, any>, children: Node[], context: PluginContext): Promise<Node[] | Node>;
}
