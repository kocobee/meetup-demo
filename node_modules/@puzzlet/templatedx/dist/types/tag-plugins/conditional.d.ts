import { Node } from "mdast";
import { TagPlugin, PluginContext } from "../tag-plugin";
export interface IfProps {
    condition: boolean;
    children: any;
}
export interface ElseIfProps {
    condition: boolean;
    children: any;
}
export interface ElseProps {
    children: any;
}
export declare const Tags: string[];
export declare class ConditionalPlugin extends TagPlugin {
    transform(props: Record<string, any>, children: Node[], context: PluginContext): Promise<Node[] | Node>;
}
