export class TagPluginRegistry {
    static register(plugin, names) {
        names.forEach((name) => {
            this.plugins.set(name, plugin);
        });
    }
    static get(name) {
        return this.plugins.get(name);
    }
    static getAll() {
        return new Map(this.plugins);
    }
    static remove(name) {
        this.plugins.delete(name);
    }
    static removeAll() {
        this.plugins.clear();
    }
}
TagPluginRegistry.plugins = new Map();
//# sourceMappingURL=tag-plugin-registry.js.map