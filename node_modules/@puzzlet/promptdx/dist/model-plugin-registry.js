export class ModelPluginRegistry {
    static register(modelPlugin, ids) {
        for (const id of ids) {
            this.plugins.set(id, modelPlugin);
        }
    }
    static getPlugin(id) {
        return this.plugins.get(id);
    }
    static removePlugin(id) {
        this.plugins.delete(id);
    }
    static clearRegistry() {
        this.plugins.clear();
    }
}
ModelPluginRegistry.plugins = new Map();
//# sourceMappingURL=model-plugin-registry.js.map